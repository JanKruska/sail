function validInds = wheelcase_ValidateChildren(children, d)
% Included for completeness' sake, however under the chosen configuration
% all possible genotypes map to valid phenotypes
    validInds = true(size(children,1));